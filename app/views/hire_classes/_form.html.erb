<%= form_with(model: hire_class, local: true) do |form| %>
  <% if hire_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hire_class.errors.count, "error") %> prohibited this hire_class from being saved:</h2>

      <ul>
      <% hire_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :class_name %>
    <%= form.text_field :class_name %>
  </div>

  <div class="field">
    <%= form.label :class_description %>
    <%= form.text_field :class_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
